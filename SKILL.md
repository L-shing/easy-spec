---
name: easy-spec
description: >-
  easy-spec: four-step gated workflow that produces four reviewable docs —
  tech design, DDL, frontend API, and open questions.
  Use when starting a feature from PRD/ownership, when the user says easy-spec /
  四步四文档 / 四步五文档 / 先出方案再写代码, or wants iterative doc review before coding.
version: "1.0.1"
updated: "2026-09-06"
author: LC
---

# easy-spec（四步四文档）

把「PRD + 分工 → 可落地实现」拆成 **4 步、4 份文档**。

**原则：** 先文档门禁、再写代码；疑问先收集、开发完再统一问人。

## 何时启用

- 用户给出 PRD / 需求文档 / 分工，要出技术方案或按流程交付
- 用户说：`easy-spec` / `四步四文档` / `四步五文档` / `先出方案再写代码`
- 中大型需求（跨表、跨接口、跨模块），不宜直接开码

**不要**用于：纯 bugfix、一行配置、已有方案只改实现细节。

## 产物目录

在目标仓库（或主责仓）创建：

```text
docs/<change-slug>/
  01-tech-design.md      # 技术方案（最重要）
  02-ddl.md              # DDL（MySQL / ES / 其它存储）
  03-api.md              # 给前端的 API 变更
  04-open-questions.md   # 疑问收集（收尾统一确认）
  sql/                   # 可选：可执行脚本（与 02 对齐）
```

`<change-slug>` 用需求号或短英文。文件名可用中文，**建议保留 01～04 序号**。

**不要**再生成 `04-dev-progress.md` / `*-dev-progress.md`（开发进度不落文档；口头或对话里同步即可）。

历史目录若仍是 `05-open-questions.md`，**继续维护该文件**，勿仅为对齐新编号而强行改名。

模版：本 Skill 目录下 `templates/`。复制后替换占位符，删掉不适用章节，禁止空话凑模板。

## 总流程（门禁）

```text
输入：PRD + 分工 +（可选）现有代码
  →【1】深度分析 → 01 技术方案 → Review 多轮 → 用户：「方案 OK」
  →【2】基于 01 → 02 DDL（+ 脚本）→ 「DDL OK」
  →【3】基于 01+02 → 03 API → 「API OK」
  →【4】开发；维护 04 疑问 → 收尾统一答疑 → 小调整
```

### 硬门禁

1. **本步未获用户认可，不得进入下一步**（用户显式说「跳过 DDL / 直接开干」除外，并在 01 记录原因）。
2. **第一步是质量中心**：宁可多轮改方案，也不要半生不熟进 DDL/API/代码。
3. **开发中**：阻塞问题可即时问；产品口径/边界项写入 `04`，**收尾批量确认**。
4. 文档与实现漂移时：改文档或改代码二选一，并在对话中说明；禁止长期双真相。
5. Git：遵循团队习惯（若约定「改完即 add、未要求不 commit」，照做）。

---

## 第一步：技术方案（01）

1. 读 PRD + 分工；写清 **In Scope / Out of Scope**。
2. **对照现有代码**（用当前工具可用的代码搜索 / 图谱 / 仓内检索）：能复用则不新建。
3. 按 `templates/01-tech-design.md` 产出；多轮 Review 至认可。

| 维度 | 要求 |
|------|------|
| 边界 | 本期做/不做、负责人 |
| **涉及服务** | 第一章必列本期碰到的全部服务（含 BFF、主责、只消费/被调的旁路），并写清各服务改动概要 |
| 外部依赖 | **单独成章**：外部 SDK/二方包、HTTP/RPC/Feign、MQ 生产/消费、其它（OSS/短信等）；无则显式写无 |
| 模块 | 新增/修改哪些域或模块（标明落在哪个服务）；职责一句话 |
| 改动点 | 逐条可执行；标注【新增/修改/删除/复用】；**文字描述必须完整、清晰**（见下「改动点写作」） |
| **链路** | 简单单向可用纯文字树；**多模块来回 / 异步多段优先 Mermaid `sequenceDiagram`**（participant=服务，`alt` 画分支）。**图与文字分支一一对应、命名一致** |
| 数据摘要 | 表/索引意图；完整 DDL 见同目录 `02-ddl.md` |
| MQ | 清单在外部依赖；编排单独成章，并落到改动点链路 |
| 定时任务 | Job 调度、扫描范围、处理摘要、幂等/失败；无则显式写无 |
| 接口摘要 | 技术方案内一张 HTTP 变更总表；完整定义见同目录 `03-api.md` |

#### 改动点写作（强制）

写 `01` 里每一条「改动点」时：

1. **文字完整、清晰**：分支条件、每步动作、写哪些库/缓存、终态怎么变，都写全；禁止「可…」「视情况」等含糊收口（除非已记入 `04` 待确认）。
2. **有分支就列齐**：如 APPROVE 空价 / 非空、REJECT 有历史 / 无历史，文字用同一套分支标签写清；缺一支即不完整。
3. **链路图与文字一致**：Mermaid / 文字树的 `alt` 分支名、步骤顺序、是否写库/ES/Redis，必须与上文逐步描述对齐；禁止图里写「旧行重算标」、正文写「完全不动」这类漂移。
4. **自检**：改完改动点后，对照「文字分支数 = 图分支数」，抽一条按文字走读图是否能走通。

交付第一步时附带：文档路径、In/Out 摘要、最多 3 个待拍板点、请回复「方案 OK」或逐条意见。

---

## 第二步：DDL（02）

1. 只依据**已认可的 01**，不要擅自扩表。
2. **文档尽量精简：主体就是可执行 DDL**（风格参考常见 `*_ddl.sql`：`CREATE` / `ALTER` + 上方 `--` 注释说明意图与注意点）。
3. 按 `templates/02-ddl.md` 产出；需要落仓脚本时，把同一份 SQL 同步到仓库约定目录（如 `resources/db/`），与 `02-ddl.md` 保持一致。
4. **`02` 只写存储结构**（MySQL / ES / 其它持久化）。**Apollo / Redis / 配置中心项写在 `01`**（外部依赖或独立配置清单），禁止塞进 `02-ddl.md`。
5. 枚举 code 陷阱用 `--` 注释写在对应语句上方即可；无表结构变更的表不要硬凑 ALTER。

---

## 第三步：前端 API（03）

1. 依据 01+02，按 `templates/03-api.md` 产出（结构对齐完整领域 API 文档，而非只写总览表）。
2. 建议章节：**文档说明 → 设计原则/路径约定 → 通用响应与枚举 → 错误码 → 接口总览 → 分端详细定义**。
3. 总览与每个详情都标明 **变更**：`新增` | `修改` | `删除` | `逻辑变更`；有 BFF 时同时写 **BFF 路径 + 下游路径**。
4. 详情须含：接口信息、请求示例、字段说明、成功响应、失败场景（错误码）。
5. 本文档只面向前端 HTTP 接口；短信/Job 等非 HTTP 不写进 03。

---

## 第四步：开发 + 04 疑问

- 默认 01～03 已认可（或用户授权跳过）。
- **不写开发进度文档**；进度在对话里同步即可。
- `04`（或历史 `05`）：**单表**维护，列固定为 `# | 问题项 | 决策 | 代码落地 | 备注`。
  - **决策**：结论原文，或填 `待确认`。
  - **代码落地**：仅 `是` / `否` / `不涉及`。
  - 文末「收尾待问」只列仍为 `待确认` 的项；开发完一次性提问。
  - 禁止再按「待确认 / 已落地 / 已确认」分多节长文。
- 实现对齐 01～03；遵守目标仓已有工程约定（若有 `AGENTS.md` / 团队规范则遵循）。

---

## 用户一句话路由

| 用户说 | Agent 做 |
|--------|----------|
| 新需求 + PRD + 分工 | 从第 1 步建 `docs/<slug>/` |
| 方案再改 / Review | 只改 01 |
| 方案 OK，出 DDL | 第 2 步 |
| DDL OK，出 API | 第 3 步 |
| API OK，开始开发 | 第 4 步，维护疑问文档（不写进度文档） |
| 跳过某步 | 记录原因后执行指定步骤 |
| 开发完了，问我疑问 | 整理疑问文档中待确认项批量提问 |
| 改 easy-spec 本身 | 按下方「维护本 Skill」升版本、写 changelog、双份同步 |

## 模版

- `templates/01-tech-design.md`
- `templates/02-ddl.md`
- `templates/03-api.md`
- `templates/04-open-questions.md`

## 维护本 Skill（迭代必做）

改本目录下 `SKILL.md` / 模版 / 示例 / README 时：

1. **升版本**（semver）：破坏性流程改 MAJOR；新增规则/步骤 MINOR；措辞/模版润色 PATCH。
2. **改 frontmatter**：同步 `version`、`updated`（当天 `YYYY-MM-DD`）、`author`（本次改动者）。`version` 必须加引号（如 `"1.0.1"`），避免 YAML 把 `1.0` 读成数字。
3. **写 `CHANGELOG.md`**：表格**顶部**追加一行（版本 / 日期 / 作者 / 一句话说明）。
4. **双份同步**：`Desktop/projects/easy-spec/` 与 `~/.cursor/skills/easy-spec/` 保持一致（先改分享包再拷到本机 skill，或两边同改）。

历史版本见 [CHANGELOG.md](CHANGELOG.md)。
