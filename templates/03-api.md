# {{领域/需求名}}API（{{英文名可选}}）

## 一、文档说明

### 1.1 文档目的

本文档定义 {{领域}} 对外提供的 **HTTP API**，覆盖 {{端：商家端 / 运营端 / …}} 本期相关接口。

### 1.2 适用范围

- 服务：`{{主责服务名}}`
- 限界上下文 / 模块：`{{context}}`
- 协议：HTTP/JSON
- 调用方式：{{如：BFF 经 Feign 调 biz / 前端直调}}
- 接口风格：{{如：动作式 + kebab-case}}

### 1.3 主要调用方

| 调用方 | 调用目的 |
|------|------|
| `{{bff-or-client}}` | {{用途摘要}} |

### 1.4 文档状态 / 变更范围（按需）

> 若本期是**存量接口改造**，在此说明对齐的技术方案；纯新建领域可删本节。

| 说明 |
|------|
| 对齐：`01-tech-design.md`；DDL：`02-ddl.md`（若有） |

---

## 二、设计原则

### 2.1 协议原则

- {{如：统一 HTTP 200，业务成败看响应体 `code`}}
- {{如：写接口幂等 / 关键状态机一句话}}

### 2.2 路径约定

前端调 BFF，BFF 再调下游时，每个接口应同时给出两层路径：

| 层级 | {{端 A}} | {{端 B}} |
|------|----------|----------|
| **BFF（前端调用）** | `/...` | `/...` |
| **下游（Feign/biz）** | `/api/internal/...` | `/api/internal/...` |

> 下方接口详情中每个接口标注 **BFF 路径** 与 **下游路径**（无下游或 BFF 本地实现时写明）。

---

## 三、通用协议约定

### 3.1 通用响应结构

```json
{
  "code": "0",
  "message": "success",
  "data": {}
}
```

### 3.2 领域枚举

#### {{枚举中文名}}（{{字段名}}）

> 枚举类：`{{EnumXxx}}`（复用已有则注明）

| 值 | 枚举 | 含义 |
|------|------|------|
| | | |

---

## 四、错误码定义

| 错误码 | 含义 | 场景 |
|------|------|------|
| `0` | 成功 | 请求处理成功 |
| `1001` | 参数校验失败 | 缺失必填项、格式非法 |
| `{{code}}` | {{含义}} | {{场景}} |

---

## 五、接口总览

### {{端 A}}接口

> 前置说明（鉴权、归属键、登录态如何传到下游等）写在表前。

| 接口 | 方法 | BFF 路径 | 下游路径 | 变更 | 用途 |
|------|------|----------|----------|------|------|
| | GET/POST/… | `/...` | `/...` 或 `—（BFF 本地）` | 新增 / 修改 / 删除 / 逻辑变更 | 一句话 |

### {{端 B}}接口

| 接口 | 方法 | BFF 路径 | 下游路径 | 变更 | 用途 |
|------|------|----------|----------|------|------|
| | | | | | |

---

## 六、{{端 A}}详细接口定义

### 6.x {{接口中文名}}

#### 接口信息

- 方法：`{{METHOD}}`
- BFF 路径（前端调用）：`/...`
- 下游路径（Feign/biz）：`/...` 或 `—（说明）`
- 调用方：{{ }}
- 变更：新增 / 修改 / 删除 / 逻辑变更（修改类写清相对旧版差什么）
- 说明：{{行为、幂等、归属、状态流转}}

#### 请求体

```json
{}
```

（GET / 无 body 时改用「请求参数」：Query / Header / 无）

#### 字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| | | 是/否 | |

#### 成功响应

```json
{
  "code": "0",
  "message": "success",
  "data": {}
}
```

#### 响应字段说明（按需）

| 字段 | 类型 | 说明 |
|------|------|------|
| | | |

#### 失败场景

| 错误码 | 说明 |
|------|------|
| | |

---

## 七、{{端 B}}详细接口定义

### 7.x {{接口中文名}}

（结构同 6.x：接口信息 → 请求 → 字段说明 → 成功响应 → 失败场景）
